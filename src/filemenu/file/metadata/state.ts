import { ref, computed, watch } from '@vue/composition-api';

// Upload Metadata (chip, wafer, dbit)
export const uploadChipInformationMenu = ref(false);
export const uploadWaferInformationMenu = ref(false);
export const uploadDbitInformationMenu = ref(false);
